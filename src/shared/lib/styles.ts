import type React from "react";

const HIDDEN_STYLE: React.CSSProperties = { visibility: "hidden" };

const MOBILE_MENU_INITIAL_STYLE: React.CSSProperties = {
  height: 0,
  visibility: "hidden",
};

export { HIDDEN_STYLE, MOBILE_MENU_INITIAL_STYLE };
